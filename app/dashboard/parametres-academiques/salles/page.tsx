import Link from "next/link";
import prisma from "@/lib/prisma";
import RetourDashboard from "../RetourDashboard";
import styles from "../parametres-academiques.module.css";
import { ajouterSalle, supprimerSalle } from "../actions";
export const dynamic="force-dynamic";
export default async function Page(){const ecole=await prisma.ecole.findFirst({select:{id:true}});if(!ecole)return <div className={styles.empty}>Créez d'abord une école.</div>;const items=await prisma.salle.findMany({where:{ecoleId:ecole.id},orderBy:{nom:"asc"}});return <div className={styles.page}><RetourDashboard/><section className={styles.section}><div className={styles.sectionHead}><div><h2>Salles de cours</h2><p>Locaux disponibles pour les cours et examens.</p></div></div><form action={ajouterSalle} className={styles.form}><input type="hidden" name="ecoleId" value={ecole.id}/><label>Code<input name="code" required/></label><label>Nom<input name="nom" required/></label><label>Type<select name="type"><option>CLASSE</option><option>LABORATOIRE</option><option>INFORMATIQUE</option><option>BIBLIOTHEQUE</option><option>POLYVALENTE</option><option>SPORT</option></select></label><label>Capacité<input type="number" name="capacite" min="1" defaultValue="40"/></label><label>Bâtiment<input name="batiment"/></label><label>Étage<input name="etage"/></label><label>Responsable<input name="responsable"/></label><div className={styles.actions}><button className={styles.primary}>Ajouter</button></div></form></section><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Code</th><th>Salle</th><th>Type</th><th>Capacité</th><th>Localisation</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.code}</td><td>{x.nom}</td><td>{x.type}</td><td>{x.capacite}</td><td>{[x.batiment,x.etage].filter(Boolean).join(" / ")||"—"}</td><td><form action={supprimerSalle}><input type="hidden" name="id" value={x.id}/><button className={styles.danger}>Supprimer</button></form></td></tr>)}</tbody></table></div></section></div>}