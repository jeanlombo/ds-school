import Link from "next/link";
import prisma from "@/lib/prisma";
import RetourDashboard from "../RetourDashboard";
import styles from "../parametres-academiques.module.css";
import { ajouterTypeCours, supprimerTypeCours } from "../actions";
export const dynamic="force-dynamic";
export default async function Page(){const ecole=await prisma.ecole.findFirst({select:{id:true}});if(!ecole)return <div className={styles.empty}>Créez d'abord une école.</div>;const items=await prisma.typeCours.findMany({where:{ecoleId:ecole.id},orderBy:{nom:"asc"}});return <div className={styles.page}><RetourDashboard/><section className={styles.section}><div className={styles.sectionHead}><div><h2>Types de cours</h2><p>Catégories réutilisables dans l'emploi du temps.</p></div></div><form action={ajouterTypeCours} className={styles.form}><input type="hidden" name="ecoleId" value={ecole.id}/><label>Code<input name="code" required/></label><label>Nom<input name="nom" required/></label><label>Couleur<input type="color" name="couleur" defaultValue="#1761A8"/></label><label className={styles.full}>Description<textarea name="description" rows={2}/></label><div className={styles.actions}><button className={styles.primary}>Ajouter</button></div></form></section><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Code</th><th>Nom</th><th>Description</th><th>Statut</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.code}</td><td><strong>{x.nom}</strong></td><td>{x.description||"—"}</td><td><span className={x.actif?styles.statusOn:styles.statusOff}>{x.actif?"Actif":"Inactif"}</span></td><td><form action={supprimerTypeCours}><input type="hidden" name="id" value={x.id}/><button className={styles.danger}>Supprimer</button></form></td></tr>)}</tbody></table></div></section></div>}