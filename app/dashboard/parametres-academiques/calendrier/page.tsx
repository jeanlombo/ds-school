import Link from "next/link";
import prisma from "@/lib/prisma";
import RetourDashboard from "../RetourDashboard";
import styles from "../parametres-academiques.module.css";
import { ajouterEvenement, supprimerEvenement } from "../actions";
export const dynamic="force-dynamic";
export default async function Page(){const [annees,items]=await Promise.all([prisma.anneeScolaire.findMany({orderBy:{dateDebut:"desc"},select:{id:true,libelle:true}}),prisma.evenementCalendrier.findMany({orderBy:{dateDebut:"asc"},include:{anneeScolaire:{select:{libelle:true}}},take:100})]);return <div className={styles.page}><RetourDashboard/><section className={styles.section}><div className={styles.sectionHead}><div><h2>Calendrier scolaire</h2><p>Vacances, jours fériés et événements pédagogiques.</p></div><Link className={styles.secondary} href="/dashboard/parametres-academiques">Centre académique</Link></div><form action={ajouterEvenement} className={styles.form}><label>Année scolaire<select name="anneeScolaireId" required>{annees.map(a=><option key={a.id} value={a.id}>{a.libelle}</option>)}</select></label><label>Titre<input name="titre" required/></label><label>Type<select name="type"><option>VACANCES</option><option>JOUR_FERIE</option><option>JOURNEE_PEDAGOGIQUE</option><option>EVENEMENT</option></select></label><label>Couleur<input type="color" name="couleur" defaultValue="#1761A8"/></label><label>Date début<input type="date" name="dateDebut" required/></label><label>Date fin<input type="date" name="dateFin" required/></label><label className={styles.full}>Description<textarea name="description" rows={2}/></label><div className={styles.actions}><button className={styles.primary}>Ajouter</button></div></form></section><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Événement</th><th>Type</th><th>Période</th><th>Année</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.titre}</strong></td><td>{x.type}</td><td>{x.dateDebut.toLocaleDateString("fr-FR")} – {x.dateFin.toLocaleDateString("fr-FR")}</td><td>{x.anneeScolaire.libelle}</td><td><form action={supprimerEvenement}><input type="hidden" name="id" value={x.id}/><button className={styles.danger}>Supprimer</button></form></td></tr>)}</tbody></table>{!items.length&&<div className={styles.empty}>Aucun événement enregistré.</div>}</div></section></div>}