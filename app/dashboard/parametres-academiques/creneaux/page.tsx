import Link from "next/link";
import prisma from "@/lib/prisma";
import RetourDashboard from "../RetourDashboard";
import styles from "../parametres-academiques.module.css";
import { ajouterCreneau, supprimerCreneau } from "../actions";
export const dynamic="force-dynamic";
export default async function Page(){const ecole=await prisma.ecole.findFirst({select:{id:true}});if(!ecole)return <div className={styles.empty}>Créez d'abord une école.</div>;const items=await prisma.creneauHoraire.findMany({where:{ecoleId:ecole.id},orderBy:{ordre:"asc"}});return <div className={styles.page}><RetourDashboard/><section className={styles.section}><div className={styles.sectionHead}><div><h2>Créneaux horaires</h2><p>Périodes de cours ordonnées.</p></div></div><form action={ajouterCreneau} className={styles.form}><input type="hidden" name="ecoleId" value={ecole.id}/><label>Nom<input name="nom" placeholder="1re période" required/></label><label>Ordre<input type="number" min="1" name="ordre" required/></label><label>Heure début<input type="time" name="heureDebut" required/></label><label>Heure fin<input type="time" name="heureFin" required/></label><div className={styles.actions}><button className={styles.primary}>Ajouter</button></div></form></section><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Ordre</th><th>Nom</th><th>Début</th><th>Fin</th><th>Statut</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.ordre}</td><td>{x.nom}</td><td>{x.heureDebut}</td><td>{x.heureFin}</td><td><span className={x.actif?styles.statusOn:styles.statusOff}>{x.actif?"Actif":"Inactif"}</span></td><td><form action={supprimerCreneau}><input type="hidden" name="id" value={x.id}/><button className={styles.danger}>Supprimer</button></form></td></tr>)}</tbody></table></div></section></div>}