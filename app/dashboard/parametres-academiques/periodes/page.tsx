import Link from "next/link";
import prisma from "@/lib/prisma";
import RetourDashboard from "../RetourDashboard";
import styles from "../parametres-academiques.module.css";
import { ajouterPeriode, supprimerPeriode } from "../actions";
export const dynamic="force-dynamic";
export default async function Page(){const [annees,items]=await Promise.all([prisma.anneeScolaire.findMany({orderBy:{dateDebut:"desc"},select:{id:true,libelle:true}}),prisma.periodeAcademique.findMany({orderBy:[{anneeScolaireId:"desc"},{ordre:"asc"}],include:{anneeScolaire:{select:{libelle:true}}}})]);return <div className={styles.page}><RetourDashboard/><section className={styles.section}><div className={styles.sectionHead}><div><h2>Périodes académiques</h2><p>Configurez trimestres ou semestres.</p></div></div><form action={ajouterPeriode} className={styles.form}><label>Année<select name="anneeScolaireId" required>{annees.map(a=><option key={a.id} value={a.id}>{a.libelle}</option>)}</select></label><label>Nom<input name="nom" placeholder="1er Trimestre" required/></label><label>Type<select name="type"><option>TRIMESTRE</option><option>SEMESTRE</option><option>SEQUENCE</option></select></label><label>Ordre<input type="number" name="ordre" min="1" defaultValue="1"/></label><label>Date début<input type="date" name="dateDebut" required/></label><label>Date fin<input type="date" name="dateFin" required/></label><label>Statut<select name="statut"><option>ACTIVE</option><option>INACTIVE</option><option>CLOTUREE</option></select></label><div className={styles.actions}><button className={styles.primary}>Enregistrer</button></div></form></section><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Nom</th><th>Type</th><th>Dates</th><th>Année</th><th>Statut</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.nom}</td><td>{x.type}</td><td>{x.dateDebut.toLocaleDateString("fr-FR")} – {x.dateFin.toLocaleDateString("fr-FR")}</td><td>{x.anneeScolaire.libelle}</td><td><span className={x.statut==="ACTIVE"?styles.statusOn:styles.statusOff}>{x.statut}</span></td><td><form action={supprimerPeriode}><input type="hidden" name="id" value={x.id}/><button className={styles.danger}>Supprimer</button></form></td></tr>)}</tbody></table></div></section></div>}