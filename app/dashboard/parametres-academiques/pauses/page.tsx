import Link from "next/link";
import prisma from "@/lib/prisma";
import RetourDashboard from "../RetourDashboard";
import styles from "../parametres-academiques.module.css";
import { ajouterPause, supprimerPause } from "../actions";
export const dynamic="force-dynamic";
export default async function Page(){const ecole=await prisma.ecole.findFirst({select:{id:true}});if(!ecole)return <div className={styles.empty}>Créez d'abord une école.</div>;const items=await prisma.pauseAcademique.findMany({where:{ecoleId:ecole.id},orderBy:{heureDebut:"asc"}});return <div className={styles.page}><RetourDashboard/><section className={styles.section}><div className={styles.sectionHead}><div><h2>Récréations et pauses</h2><p>Plages bloquées dans l'emploi du temps.</p></div></div><form action={ajouterPause} className={styles.form}><input type="hidden" name="ecoleId" value={ecole.id}/><label>Nom<input name="nom" required/></label><label>Type<select name="type"><option>RECREATION</option><option>DEJEUNER</option><option>PAUSE</option></select></label><label>Début<input type="time" name="heureDebut" required/></label><label>Fin<input type="time" name="heureFin" required/></label><label>Couleur<input type="color" name="couleur" defaultValue="#F59E0B"/></label><div className={styles.actions}><button className={styles.primary}>Ajouter</button></div></form></section><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Nom</th><th>Type</th><th>Horaire</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.nom}</td><td>{x.type}</td><td>{x.heureDebut} – {x.heureFin}</td><td><form action={supprimerPause}><input type="hidden" name="id" value={x.id}/><button className={styles.danger}>Supprimer</button></form></td></tr>)}</tbody></table></div></section></div>}