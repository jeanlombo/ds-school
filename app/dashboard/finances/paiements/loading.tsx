export default function Loading() {
  return (
    <div
      style={{
        minHeight: 380,
        display: "grid",
        placeItems: "center",
        color: "#64748b",
        fontWeight: 800,
      }}
    >
      Chargement des paiements scolaires...
    </div>
  );
}
