import Link from "next/link";import {redirect,notFound} from "next/navigation";import AdminShell from "@/components/admin/AdminShell";import prisma from "@/lib/prisma";import {obtenirOuCreerEcole} from "@/lib/ecole";import {obtenirUtilisateurConnecte} from "@/lib/session";import RetourDashboard from "../../RetourDashboard";import {modifierModeleBulletin} from "../../actions";import FormulaireModele from "../FormulaireModele";import styles from "../../centre-academique.module.css";export default async function Page({params}:{params:Promise<{id:string}>}){const u=await obtenirUtilisateurConnecte();if(!u)redirect('/connexion');const e=await obtenirOuCreerEcole();const {id}=await params;const m=await prisma.modeleBulletin.findFirst({where:{id:Number(id),ecoleId:e.id},include:{versions:{orderBy:{numeroVersion:'desc'},take:10}}});if(!m)notFound();return <AdminShell utilisateur={u} titre={`Modifier : ${m.nom}`} description="Chaque sauvegarde crée une version historique." action={<Link className={styles.btnSecondary} href={`/dashboard/centre-academique/modeles-bulletins/${m.id}/apercu`}>Voir l’aperçu</Link>}><div className={styles.page}><RetourDashboard/><section className={styles.panel}><FormulaireModele action={modifierModeleBulletin} modele={m}/></section><section className={styles.panel}><h2>Historique des versions</h2><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Version</th><th>Date</th><th>Commentaire</th></tr></thead><tbody>{m.versions.map(v=><tr key={v.id}><td>V{v.numeroVersion}</td><td>{v.createdAt.toLocaleString('fr-FR')}</td><td>{v.commentaire||'—'}</td></tr>)}</tbody></table></div></section></div></AdminShell>}
