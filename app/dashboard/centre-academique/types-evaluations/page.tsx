import { redirect } from "next/navigation";import { Plus,Trash2 } from "lucide-react";import AdminShell from "@/components/admin/AdminShell";import prisma from "@/lib/prisma";import { obtenirOuCreerEcole } from "@/lib/ecole";import { obtenirUtilisateurConnecte } from "@/lib/session";import RetourDashboard from "../RetourDashboard";import { creerTypeEvaluation,supprimerTypeEvaluation } from "../actions";import styles from "../centre-academique.module.css";export const dynamic='force-dynamic';
export default async function Page({searchParams}:{searchParams:Promise<{succes?:string;erreur?:string}>}){const u=await obtenirUtilisateurConnecte();if(!u)redirect('/connexion');const e=await obtenirOuCreerEcole();const p=await searchParams;const types=await prisma.typeEvaluation.findMany({where:{ecoleId:e.id},orderBy:{nom:'asc'},include:{_count:{select:{evaluations:true}}}});return <AdminShell utilisateur={u} titre="Types d’évaluations" description="Configurez les catégories, barèmes et coefficients par défaut."><div className={styles.page}><RetourDashboard/>{p.succes&&<div className={styles.message}>Opération effectuée avec succès.</div>}{p.erreur&&<div className={styles.erreur}>Code déjà utilisé ou champs incomplets.</div>}<div className={styles.two}><section className={styles.panel}><div className={styles.panelHead}><div><h3>Nouveau type</h3><p>Ex. Interrogation, Devoir, Examen.</p></div></div><form action={creerTypeEvaluation} className={styles.form}><label>Nom<input name="nom" required/></label><label>Code<input name="code" required/></label><label>Coefficient<input name="coefficient" type="number" step="0.25" min="0.25" defaultValue="1"/></label><label>Note maximale<input name="noteMax" type="number" min="1" defaultValue="20"/></label><label className={styles.full}>Couleur<input name="couleur" type="color" defaultValue="#1761A8"/></label><div className={styles.formActions}><button className={styles.btn}><Plus size={17}/> Ajouter</button></div></form></section><section className={styles.panel}><div className={styles.panelHead}><h3>Types configurés</h3></div><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Type</th><th>Coeff.</th><th>Barème</th><th>Utilisations</th><th></th></tr></thead><tbody>{types.map(t=><tr key={t.id}><td><b style={{borderLeft:`5px solid ${t.couleur}`,paddingLeft:9}}>{t.nom}</b><br/><small>{t.code}</small></td><td>{Number(t.coefficientParDefaut)}</td><td>/{Number(t.noteMaxParDefaut)}</td><td>{t._count.evaluations}</td><td><form action={supprimerTypeEvaluation}><input type="hidden" name="id" value={t.id}/><button className={styles.btnDanger} title="Supprimer"><Trash2 size={16}/></button></form></td></tr>)}</tbody></table></div></section></div></div></AdminShell>}
