import { redirect } from "next/navigation";
import { BookOpenCheck, Layers3, Power } from "lucide-react";
import { obtenirUtilisateurConnecte } from "@/lib/session"; import { obtenirOuCreerEcole } from "@/lib/ecole"; import { prisma } from "@/lib/prisma";
import AdminShell from "@/components/admin/AdminShell"; import BoutonSoumission from "@/components/admin/BoutonSoumission"; import styles from "@/components/admin/admin.module.css"; import { basculerSection, creerSection } from "./actions";
export default async function Sections({searchParams}:{searchParams:Promise<{succes?:string;erreur?:string}>}){const utilisateur=await obtenirUtilisateurConnecte();if(!utilisateur)redirect("/connexion");const ecole=await obtenirOuCreerEcole();const sections=await prisma.section.findMany({where:{ecoleId:ecole.id},include:{_count:{select:{classes:true}}},orderBy:{nom:"asc"}});const query=await searchParams;
return <AdminShell utilisateur={utilisateur} titre="Sections scolaires" description="Organisez les cycles et filières de votre établissement.">{query.succes&&<div className={styles.message}>La section a été ajoutée avec succès.</div>}<div className={styles.deuxColonnes}><section className={styles.panneau}><div className={styles.panneauEntete}><div><h2>Liste des sections</h2><p>{sections.length} section(s) configurée(s)</p></div><Layers3 size={22}/></div>{sections.length?<div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Section</th><th>Code</th><th>Classes</th><th>Statut</th><th>Action</th></tr></thead><tbody>{sections.map(s=><tr key={s.id}><td><strong>{s.nom}</strong><br/><small>{s.description}</small></td><td>{s.code}</td><td>{s._count.classes}</td><td><span className={`${styles.badge} ${s.statut!=="active"?styles.badgeInactif:""}`}>{s.statut}</span></td><td><form action={basculerSection}><input type="hidden" name="id" value={s.id}/><input type="hidden" name="statut" value={s.statut}/><button className={s.statut==="active"?styles.boutonDanger:styles.boutonSecondaire}><Power size={15}/></button></form></td></tr>)}</tbody></table></div>:<div className={styles.vide}><Layers3 size={38}/><p>Aucune section disponible.</p></div>}</section><section className={styles.panneau}><div className={styles.panneauEntete}><div><h2>Ajouter une section</h2><p>Ex. Maternelle, Primaire, Secondaire.</p></div><BookOpenCheck size={22}/></div><form action={creerSection} className={styles.panneauCorps}><div className={styles.formGrille}><div className={styles.champ}><label>Nom *</label><input name="nom" required placeholder="Primaire"/></div><div className={styles.champ}><label>Code *</label><input name="code" required placeholder="PRIM"/></div><div className={`${styles.champ} ${styles.champLarge}`}><label>Description</label><textarea name="description" placeholder="Description facultative"/></div></div><div className={styles.actions}><BoutonSoumission texte="Ajouter la section"/></div></form></section></div></AdminShell>}
